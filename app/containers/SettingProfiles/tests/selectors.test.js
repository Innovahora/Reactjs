// import { selectSettingProfilesDomain } from '../selectors';

describe('selectSettingProfilesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
