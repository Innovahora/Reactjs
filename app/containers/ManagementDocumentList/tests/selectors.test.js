// import { selectManagementDocumentListDomain } from '../selectors';

describe('selectManagementDocumentListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
