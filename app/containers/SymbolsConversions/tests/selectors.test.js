// import { selectSymbolsConversionsDomain } from '../selectors';

describe('selectSymbolsConversionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
