// import { selectSettingAreasDomain } from '../selectors';

describe('selectSettingAreasDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
