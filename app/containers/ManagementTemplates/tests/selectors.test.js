// import { selectManagementTemplatesDomain } from '../selectors';

describe('selectManagementTemplatesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
