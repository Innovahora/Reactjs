/*
 * ManagementTemplates Messages
 *
 * This contains all the text for the ManagementTemplates container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManagementTemplates';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManagementTemplates container!',
  },
});
