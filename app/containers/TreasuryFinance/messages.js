/*
 * TreasuryFinance Messages
 *
 * This contains all the text for the TreasuryFinance container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TreasuryFinance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TreasuryFinance container!',
  },
});
