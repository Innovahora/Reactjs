// import { selectTreasuryFinanceDomain } from '../selectors';

describe('selectTreasuryFinanceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
