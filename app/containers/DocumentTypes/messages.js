/*
 * DocumentTypes Messages
 *
 * This contains all the text for the DocumentTypes container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentTypes';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentTypes container!',
  },
});
