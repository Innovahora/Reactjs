// import { selectSettingCentersCostsDomain } from '../selectors';

describe('selectSettingCentersCostsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
