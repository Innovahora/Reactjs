// import { selectSettingUsersDomain } from '../selectors';

describe('selectSettingUsersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
