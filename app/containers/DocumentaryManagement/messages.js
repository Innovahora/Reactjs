/*
 * DocumentaryManagement Messages
 *
 * This contains all the text for the DocumentaryManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentaryManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentaryManagement container!',
  },
});
