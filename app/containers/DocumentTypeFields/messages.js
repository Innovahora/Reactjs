/*
 * DocumentTypeFields Messages
 *
 * This contains all the text for the DocumentTypeFields container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentTypeFields';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentTypeFields container!',
  },
});
