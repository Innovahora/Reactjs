/*
 * DocumentUploadForm Messages
 *
 * This contains all the text for the DocumentUploadForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DocumentUploadForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentUploadForm component!',
  },
});
