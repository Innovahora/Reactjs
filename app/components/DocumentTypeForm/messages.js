/*
 * DocumentTypeForm Messages
 *
 * This contains all the text for the DocumentTypeForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DocumentTypeForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentTypeForm component!',
  },
});
