/*
 * FormDocumentTypeFields Messages
 *
 * This contains all the text for the FormDocumentTypeFields component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormDocumentTypeFields';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormDocumentTypeFields component!',
  },
});
